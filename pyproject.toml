[project]
name = "labrad4_pkgs"
version = "0.1.0"
dependencies = [
  "numpy",
  "scipy",
  "matplotlib",
  "ipython",
  "ipykernel",
  "twisted",
  "Cython",
  "pyserial",
  "future",
  "requests",
  "pandas",
  "tqdm",
  "scikit-learn",
  "numba",
  "pyvisa",
  "labcodes",
  "logqbit",
  "WaveMonitor",
  "pylabrad",
  "qcodes>=0.58.0",
  "scipy-stubs~=1.17.1",
  "pydantic>=2.13.4",
]
requires-python = ">=3.11"
authors = [{ name = "Qiujv", email = "qiujv@outlook.com" }]
description = "Packages simplifying experiment scripts with LabRAD4."
readme = "readme.md"
classifiers = [
  "Programming Language :: Python :: 3",
  "License :: OSI Approved :: MIT License",
  # "Operating System :: OS Independent",
]

[project.urls]
Homepage = "http://git.iqasz.com/qiujv/labrad4"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["control", "lab4/src/lab4", "vendor/MMCSDriver"]

[dependency-groups]
dev = [
  "pytest>=8.0",
  "pytest-cov>=5.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
  "hardware: requires an explicitly enabled connection to laboratory hardware",
]
